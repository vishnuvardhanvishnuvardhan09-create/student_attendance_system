# ==========================================================
# Server Configuration
# ==========================================================
server.port=8080

# ==========================================================
# Database Configuration (Plain JDBC Connection to MySQL)
# ==========================================================
spring.datasource.url=jdbc:mysql://localhost:3306/student_attendance_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Vishnu8688@.1
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ==========================================================
# JPA / Hibernate Configuration
# ==========================================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# ==========================================================
# Global CORS Configuration
# Comma-separated allowed origins (Local React development and future Vercel deployment placeholder)
# ==========================================================
app.cors.allowed-origins=http://localhost:3000,https://student-attendance-system-frontend.vercel.app

# ==========================================================
# Campus Geolocation Configuration for Attendance Geofence
# ==========================================================
campus.latitude=13.434567569657812
campus.longitude= 79.50967886558122
campus.radius-meters=100.0
